{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/voice-match-score", "destination": "/api/persona-signal" },
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
